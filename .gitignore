# Hugo build output & caches
/public/
/resources/_gen/
.hugo_build.lock
hugo_stats.json

# OS / editor noise
.DS_Store
*.log

# Local secrets (docker-compose)
.env
# Analytics dashboard credentials (create with: htpasswd -c deploy/htpasswd <user>)
deploy/htpasswd

# Node (only if tooling is added later)
node_modules/

# IDE / tooling
.idea/
*.iml
.serena/
/tmp/

# Go build output (contact-service is built straight to /opt in production)
/contact-service/contact
/contact-service/contact-service
