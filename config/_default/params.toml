# Site-wide parameters. Values marked TODO must be confirmed by the owner.

description = "MentesIT / FreeIT — egyedi szoftverfejlesztés, weboldalak, rendszerüzemeltetés és tárhely egy helyen."
author = "MentesIT"

# Contact details (also shown in the status bar / footer).
# email is the Cloudflare-routed address that forwards to the owner's mailbox.
email = "info@mentesit.eu"
phone = "+36 70 434 2846"
location = "Fonyód, Magyarország"

# Contact form endpoint — wired up in milestone M6. Self-hosted /api/contact by default.
formEndpoint = "/api/contact"

# Brand
brandHu = "MentesIT"
brandEn = "FreeIT"
domain = "mentesit.eu"
established = "2026"

# Brand accent (kept in sync with assets/scss/_tokens.scss)
accent = "#5BE9B9"

[social]
  github = ""
  linkedin = ""

# Legal entity / structured data (single source of truth for JSON-LD).
[company]
  legalName     = "MentesIT Kft."
  regNumber     = "14-09-318034"      # cégjegyzékszám
  taxId         = "28998596-2-14"     # adószám
  streetAddress = "Erdősor utca 14."
  postalCode    = "8640"
  locality      = "Fonyód"
  countryCode   = "HU"
  founded       = "2021"
